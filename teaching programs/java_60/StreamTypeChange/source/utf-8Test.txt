在Windows电脑的控制台中输入中文字符后，使用Scanner类的.nextLine()方法获取到的数据是经过控制台默认编码方式（通常是GBK或GB2312）编码后的二进制流。

在Windows控制台中，默认情况下，控制台使用本地操作系统的默认字符编码（例如GBK或GB2312）来解析输入的字符。当你输入中文字符时，它们会被转换为相应的编码格式，然后才被传递给Java程序。

因此，当你使用Scanner类的.nextLine()方法获取输入时，它将返回经过控制台默认编码方式编码后的二进制流。若要将其转换回正确的Unicode字符，你可以使用适当的字符编码方式进行解码。